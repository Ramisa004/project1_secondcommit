import 'package:flutter/material.dart';

class PanicAlertScreen extends StatelessWidget {
  const PanicAlertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Panic Alert Screen"),
      ),
    );
  }
}
